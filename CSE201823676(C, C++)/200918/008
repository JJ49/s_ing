#include <stdio.h>

int main(void) {
  int time, hour, min, sec;

	printf("초를 입력하시오: ");
	scanf("%d", &time);
	
	sec = time % 60;
	time /= 60;
	min = time % 60;
	hour = time / 60;

	printf("%d시간 %d분 %d초\n", hour, min, sec);

  return 0;
}
